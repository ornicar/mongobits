# Export selected documents from a Mongo DB

Inspired by https://github.com/applidget/mongo-partial-dump

But in NodeJS and without requiring ObjectId.


## Install

`npm install`

## Usage

Tweak config.js, then `node index.js`
